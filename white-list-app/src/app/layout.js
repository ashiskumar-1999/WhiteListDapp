export const metadata = {
  title: "WhiteListDapp",
  description: "Interface to register acccount for whitelisting",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
